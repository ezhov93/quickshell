## 1. Подготовка и загрузчик

- [ ] 1.1 Зафиксировать карту текущих деклараций из `extra/hypr/hyprland.lua` и целевое дерево `extra/hypr/modules/`; проверить, что ни одна команда или настройка не потеряна.
- [ ] 1.2 Реализовать явный загрузчик модулей из `hyprland.lua` с путём от entrypoint и диагностикой ошибки; проверить статически порядок загрузки.
- [ ] 1.3 Вынести общие команды, `mainMod` и идентификатор `lpde-nmtui` в единый контекст; проверить отсутствие противоречащих дубликатов.

## 2. Разбиение профиля

- [ ] 2.1 Вынести env и автозапуск в `modules/session.lua`, а monitor/settings/gesture — в `modules/settings.lua`; проверить сохранение деклараций.
- [ ] 2.2 Вынести приложения и window rules в `modules/applications.lua` и `modules/windows.lua`; проверить `lpde-nmtui`, обычный Foot и правила XWayland.
- [ ] 2.3 Вынести desktop/input keybinds, multimedia/brightness/media и Quickshell IPC в соответствующие модули; проверить полный список сочетаний и команд.
- [ ] 2.4 Удалить из entrypoint старые inline-декларации, оставив только загрузку модулей и необходимую инициализацию; проверить, что файл остаётся единственной точкой входа.

## 3. Документация и проверки

- [ ] 3.1 Обновить `docs/architecture.md`, `docs/testing.md` и `docs/spec-status.md` описанием дерева Lua-профиля и статических/ручных границ проверки.
- [ ] 3.2 Выполнить `openspec validate "refactor-hyprland-lua-architecture" --strict`, `openspec validate --specs --strict`, `git diff --check` и отдельную whitespace-проверку новых файлов.
- [ ] 3.3 Владелец подготовленной графической сессии вручную проверяет загрузку профиля и отсутствие функциональных регрессий; агент сессию не запускает и не перезапускает.
