## 1. Идентификация и правило окна

- [x] 1.1 Добавить `--app-id=lpde-nmtui` в `Config.networkSettingsCommand`, сохранив Foot и `nmtui`; проверить массив аргументов статически и через `foot --help`.
- [x] 1.2 Добавить в `extra/hypr/hyprland.lua` именованное правило с точным match по `lpde-nmtui` и эффектом `float = true`; проверить синтаксис и отсутствие правила для общего класса `foot`.
- [x] 1.3 Включить `general.resize_on_border = true` и `general.snap` со значениями `enabled = true`, `window_gap = 4`, `monitor_gap = 5`, `respect_gaps = true`; проверить единственное итоговое значение параметров и отсутствие включённого `border_overlap`.
- [x] 1.4 Добавить к запуску `nmtui` локальное переопределение Foot `main.pad=0x0`; сохранить глобальный `pad` для обычных терминалов и проверить опцию через `foot --help`.

## 2. Документация и статические проверки

- [x] 2.1 Обновить ручной checklist Bar в `docs/testing.md`: `nmtui` открывается floating, его class равен `lpde-nmtui`, а обычный Foot остаётся tiled; проверить, что агенту не поручается запуск графической сессии.
- [x] 2.2 Обновить `docs/spec-status.md` источниками и статусом новой декларации без утверждения о пройденной runtime-проверке.
- [x] 2.3 Запустить `openspec validate "float-nmtui-window" --strict`, `openspec validate --specs --strict` и `git diff --check`; результаты записать как passed/failed/not run/blocked. Статические проверки passed; runtime not run.
- [x] 2.4 Обновить checklist и карту статуса: отдельно проверять отсутствие полей у `nmtui` и сохранение отступа обычного Foot; не заявлять runtime-проверку до сообщения владельца сессии.

## 3. Ручная проверка владельцем сессии

- [x] 3.1 В подготовленной Hyprland-сессии применить Lua-профиль, открыть сетевые настройки из панели и подтвердить через `hyprctl clients`, что окно `lpde-nmtui` имеет floating-режим; результат сообщает пользователь.
- [x] 3.2 Изменить размер плавающего окна перетаскиванием за границу, затем проверить его прилипание к другому окну и краю монитора с сохранением отступов; результат сообщает пользователь.
- [x] 3.3 Открыть обычный Foot и подтвердить, что правило `nmtui` к нему не применяется; результат сообщает пользователь.
