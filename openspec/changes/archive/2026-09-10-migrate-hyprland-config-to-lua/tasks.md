## 1. Исследование загрузчика и расхождений

- [x] 1.1 Определить поставщика API `hl`, способ запуска Lua-профиля, поддерживаемую версию Hyprland и ожидаемое имя/путь конфигурации; записать команды и ограничения, не добавляя новую зависимость. Проверено: native Lua manager Hyprland 0.56.2, `hyprland --verify-config --config <file>.lua`; отдельной команды `hl` нет.
- [x] 1.2 Составить таблицу всех различий между `extra/hypr/hyprland.conf` и `/home/ezhov/.config/hypr/hyprland.lua`: monitor, env, autostart, input, appearance, layout, gestures, workspaces, keybinds, screenshot, media, brightness, IPC и window rules; отдельно отметить конфликт `Super+Shift+S`, `Super+Shift+E`, группы и дополнительные команды. Расхождения зафиксированы в `design.md`; проверка референса выявила неподдерживаемый helper `hl.dsp.window.group`.
- [x] 1.3 Зафиксировать решения по каждому изменению поведения и определить, остаётся ли `.conf` legacy-файлом или удаляется после подтверждения нового loader; не менять код до завершения этого шага. Решено: Lua становится каноническим профилем, `.conf` удаляется после успешной статической проверки, обязательные session-декларации сохраняются, `Super+Shift+S` используется для screenshot в буфер, `PRINT` — для сохранения PNG, группы удаляются.

## 2. Реализация Lua-профиля

- [x] 2.1 Создать канонический Lua-файл в `extra/hypr/` с форматом и точкой входа, совместимыми с подтверждённым loader; перенести согласованные настройки референса. Создан `extra/hypr/hyprland.lua` для native Lua manager Hyprland.
- [x] 2.2 Сохранить обязательные декларации действующей `session`-спеки: четыре `exec-once`, три session environment variables, input, лёгкие параметры композитора, idle/shell интеграцию и активные desktop keybinds. Автозапуск реализован в `hyprland.start`, остальные декларации перенесены в Lua; `notifications dismiss_all` и `idle toggle` с hotkey отложены.
- [x] 2.3 Устранить дублирующиеся и конфликтующие keybinds, особенно `Super+Shift+S`; убедиться, что screenshot, exit/stop session и активные desktop/shell binds имеют однозначные назначения согласно принятой таблице. Группы, scratchpad, `Super+N` и `Super+C` удалены; `PRINT` сохраняет screenshot, `Super+Shift+S` копирует его в буфер; выход выполняется через `uwsm stop` на `Super+Shift+M`.
- [x] 2.4 Обновить роль `extra/hypr/hyprland.conf` только после подтверждения loader: удалить его как основной профиль либо явно обозначить legacy и обеспечить безопасный откат; не менять остальные файлы `extra/hypr/`. Loader подтверждён через `hyprland --verify-config`; старый `.conf` удалён.

## 3. Проверка и документация

- [x] 3.1 Выполнить статическое сравнение итогового Lua с действующей session-спекой и таблицей расхождений; проверить синтаксис Lua, баланс/структуру деклараций и отсутствие неизвестных API по доступной схеме. `lua loadfile` и `hyprland --verify-config` прошли; неподдержанный helper групп удалён вместе с функциональностью групп.
- [x] 3.2 Выполнить `openspec validate "migrate-hyprland-config-to-lua" --strict` и `openspec validate --specs --strict`. Обе проверки прошли.
- [x] 3.3 Владелец окружения вручную, по checklist из `docs/testing.md`, в отдельной Hyprland-сессии с recovery-путём проверил загрузку профиля, запуск обязательных компонентов, раскладки, активные desktop/shell keybinds и отсутствие ошибок; screenshot, desktop keybinds, автозапуск, внешний вид и idle-политика пройдены. Для `Super+O` подтверждён вызов и открытие окна MonitorManager; его функциональность вынесена в отдельную задачу. DND и функциональность MonitorManager не являются критериями закрытия этого change. Агент не поднимает и не изменяет графическую сессию.
- [x] 3.4 Обновить `docs/architecture.md` и `docs/spec-status.md` по фактическому результату, сохранив границу примерного неустанавливаемого профиля и не заявляя полную установку/совместимость Arch и Debian. Документы обновлены; runtime-ограничения и G05/G06/G09 сохранены.
- [x] 3.5 Выполнить `git diff --check`, отдельную whitespace-проверку новых файлов и `git status --short`; подтвердить, что `install.sh`, QML, `hypridle.conf`, `hyprlock.conf`, `hyprpaper.conf` и `monitors.conf` не изменены без отдельного решения. Проверки whitespace прошли; изменения этого change — Lua, удалённый старый профиль, согласованная документация и OpenSpec-артефакты; отдельные уже существующие изменения `hyprlock`/документации сохранены.
