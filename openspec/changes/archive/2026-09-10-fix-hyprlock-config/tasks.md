## 1. Исправление конфигурации

- [x] 1.1 Добавить отсутствующее открытие категории `general` в `extra/hypr/hyprlock.conf` и завершающий перевод строки, не меняя остальные значения; проверить баланс категорий и убедиться по diff, что `hide_cursor` и `immediate_render` остались равны `true`, а блоки `background`, `label` и `input-field` не изменились.
- [x] 1.2 Сверить все объявленные параметры исправленного файла со схемой зафиксированной версии hyprlock; проверить, что для каждой строки определена поддерживаемая категория и статическая проверка не сообщает неизвестных параметров или непарных скобок.

## 2. Контролируемая runtime-проверка

- [x] 2.1 Во внешне подготовленной отдельной безопасной Hyprland-сессии с установленными hyprlock и рабочим поставщиком аутентификации, известными корректными учётными данными и независимым способом восстановления записать дистрибутив, оборудование, версии Hyprland, hyprlock и значимых библиотек; агент не может поднять такую сессию сам, поэтому отсутствие предоставленного окружения проверяется и фиксируется как `заблокировано`. Ручная проверка владельцем подтверждена; окружение: Arch Linux, Intel Celeron N3450, x86_64, Hyprland 0.56.2, hyprlock 0.9.6, hypridle 0.1.8.
- [x] 2.2 В подготовленной сессии запустить `hyprlock --config <абсолютный-путь-к-extra/hypr/hyprlock.conf>` и проверить отсутствие ошибок конфигурации, скрытый курсор, немедленную отрисовку, фон, часы, дату и поле ввода на всех доступных выходах; сохранить журнал и безопасный визуальный материал либо явно записать причину отсутствия screenshot. Запуск в единственной живой сессии агента не считается допустимым выполнением. Пользователь подтвердил успешную загрузку и отображение всех элементов; screenshot и отдельный журнал не переданы, поскольку проверка выполнена вручную без сохранения этих материалов.
- [x] 2.3 В том же запуске ввести неверные учётные данные и проверить, что сессия остаётся заблокированной и допускает повторную попытку, затем ввести корректные данные и проверить возврат в сессию; агент не может достоверно выполнить lock/unlock и PAM-проверку без внешнего recovery-пути, поэтому при его отсутствии задача остаётся незавершённой со статусом `заблокировано`. Пользователь подтвердил успешное выполнение обеих ветвей аутентификации.

## 3. Согласование документации

- [x] 3.1 Обновить раздел сессии в `docs/architecture.md`, сохранив границу примерного неустанавливаемого профиля и остальные ограничения; проверить, что документ больше не называет исправленное начало `hyprlock.conf` подозрительным и не заявляет установку полного профиля.
- [x] 3.2 Обновить строку требования `session` и G04 в `docs/spec-status.md` по фактическим статической и runtime-проверкам; проверить, что выполненное, невыполненное и заблокированное различены и полная проверка таймеров, sleep и inhibitor не объявлена пройденной.
- [x] 3.3 После успешного выполнения обязательной runtime-проверки убрать G04 из будущей работы в `docs/roadmap.md`; проверить, что G05, G06, G09 и этап полного lifecycle сессии остались без изменений. G04 удалён из roadmap; G05, G06 и G09 сохранены.

## 4. Сквозная проверка и передача

- [x] 4.1 Выполнить `openspec validate "fix-hyprlock-config" --strict` и `openspec validate --specs --strict`; проверить, что change и все пять baseline-спек проходят без ошибок, а delta сохраняет полное исходное требование и оба прежних сценария.
- [x] 4.2 Выполнить `git diff --check`, отдельную whitespace-проверку новых untracked-файлов и `git status --short`; проверить, что изменения ограничены `extra/hypr/hyprlock.conf`, тремя согласованными документами и артефактами change, а `hypridle.conf`, `hyprland.conf`, `install.sh`, QML и исторический `docs/architecture-review.md` не изменены.
- [x] 4.3 Передать результат с описанием изменения поведения, статусом каждой проверки, версиями среды, известными пробелами и наличием либо обоснованным отсутствием screenshot; не помечать change полностью выполненным, если обязательная runtime-проверка заблокирована или не выполнялась.
