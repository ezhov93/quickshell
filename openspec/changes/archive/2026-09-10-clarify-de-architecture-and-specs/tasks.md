## 1. Продуктовый контекст и архитектура

- [x] 1.1 Переписать `docs/project-context.md` как краткий продуктовый контекст по D1: цели, владение, аппаратные ограничения, текущая поставка, целевые Arch/Debian и нецели. Проверка: полная установка DE не объявлена существующей, нет действующих ссылок на корневой `specs/` и нет большого дерева несуществующих runtime-каталогов.
- [x] 1.2 Создать `docs/architecture.md` с девятью разделами D3 и корректной схемой зависимостей. Проверка: каждый текущий путь существует; процессы, локальное/системное состояние, внешние API, данные, установка и сессия описаны раздельно; будущие решения не выданы за текущие.
- [x] 1.3 Создать `docs/adr/0001-de-product-boundary.md` с контекстом, решением, статусом, альтернативами и последствиями. Проверка: записана существующая граница продукта и владения, а не новые обязательные зависимости.
- [x] 1.4 Создать `docs/adr/0002-modular-quickshell-shell.md` по D3. Проверка: обоснован модульный состав одного процесса, исправлены зависимости, адаптеры необязательны, известное несоответствие UI не скрыто и UWSM не выбран автоматически.

## 2. Карта требований и известных пробелов

- [x] 2.1 Создать `docs/spec-status.md` с форматом и статусами D5; заполнить пять строк `desktop-environment`. Проверка: каждому требованию соответствует строка с основанием, источником, соответствием и методом проверки; цель продукта не помечена как испытанная поставка.
- [x] 2.2 Добавить в `docs/spec-status.md` семь строк `architecture` с источниками D5. Проверка: архитектурные решения отделены от runtime-фактов; нарушение границ UI и исключение опроса яркости отражены явно.
- [x] 2.3 Добавить в `docs/spec-status.md` десять строк `shell`. Проверка: все сценарии связаны с проверкой или явным отсутствием проверки, markup отмечен как несоответствие, а сохранение настроек монитора и обоев не названо автоматическим откатом.
- [x] 2.4 Добавить в `docs/spec-status.md` семь строк `system-package-management`. Проверка: версия/импорты/шрифт не названы автоматически проверяемыми; резервирование отличается от переноса состояния, а просмотр скрипта — от испытания установки.
- [x] 2.5 Добавить в `docs/spec-status.md` семь строк `session`. Проверка: декларации профиля не названы подтверждённым запуском; блокировка, переносимость и наследование окружения отмечены с фактическими ограничениями.
- [x] 2.6 Добавить в `docs/spec-status.md` записи G01–G09 из D5 с датой, источниками, типом пробела и ссылками из соответствующих строк требований. Проверка: существующие дефекты отличаются от кандидатов новых возможностей; ни один пробел не помечен исправленным данным документационным change.

## 3. Правила работы и следующие этапы

- [x] 3.1 Создать `docs/roadmap.md` по D7. Проверка: текущий этап отделён от последующих, первые автоматические проверки сопровождают безопасную установку, выбор Debian/UWSM/framework не принят за пользователя, критическое исправление блокировки не обязано ждать всей перестройки.
- [x] 3.2 Обновить `AGENTS.md` на английском по D2 и D6. Проверка: указан порядок чтения, актуальные `modules/`, `services/Brightness.qml`, `config/Theme.qml` и `qs.config`; нет прежних путей как действующих инструкций; сохранены ограничения оборудования, пользовательских изменений и живых побочных эффектов.
- [x] 3.3 Сократить контекст `openspec/config.yaml` и добавить правила для `proposal`, `specs`, `design`, `tasks` по D6, не меняя схему. Проверка: `openspec instructions tasks --change "clarify-de-architecture-and-specs" --json` успешно читает конфигурацию и возвращает правила; ссылки существуют после завершения набора документов, архитектура не скопирована целиком.
- [x] 3.4 Обновить `docs/testing.md` по D6. Проверка: существующие процедуры отделены от их результатов и будущих тестов; различены отсутствующие QML-импорты и внешние устройства/службы; добавлен формат отчёта и запрет считать недоступную обязательную проверку пройденной; нет подмены `HOME` рабочего процесса и выдуманной автоматизации.
- [x] 3.5 Обновить `docs/openspec-workflow.md` по D2/D6: применение выбранного change, проверки основной спеки и дельт, синхронизация без автоматического архивирования, остановка при конфликте и различие исправления описания и нового поведения. Проверка: нет обязательного порядка всех слоёв для любой задачи; сохранена пользовательская пометка о моделях без пересмотра её содержания.
- [x] 3.6 Создать `docs/README.md` как указатель D1. Проверка: все ссылки ведут в существующие документы или спеки, исторический разбор и `PLANS.md` явно не представлены действующими заданиями, подробные правила не продублированы.
- [x] 3.7 Уточнить вступление/область установки в `README.md` и добавить ссылку на указатель; в `PLANS.md` добавить только статусную оговорку и ссылку на roadmap. Проверка: команды README соответствуют неизменённому коду; исходные планы не выполнены, не удалены и не выданы за согласованный change.

## 4. Синхронизация документированного контракта

- [x] 4.1 Использовать `openspec-sync-specs` для переноса пяти дельт данного change в основные спеки, без архивирования. Проверка: сохранены пять путей capability, все 36 заголовков требований и исходные имена сценариев; перенесены полные блоки из дельт, а не их пересказ; новых runtime-возможностей не добавлено.
- [x] 4.2 Отдельно заменить `Purpose` пяти основных спек точными текстами из D4. Проверка: области текущей поставки, архитектурных ограничений и примерного профиля явно различаются; `openspec validate --specs --strict` проходит.

## 5. Сквозная проверка и передача результата

- [x] 5.1 Проверить согласованность указателя, контекста, архитектуры, ADR, основных спек и карты статуса. Проверка: таблица содержит все 36 требований, сценарии с известными пробелами не помечены пройденными, локальные Markdown-ссылки существуют, старые пути остаются только в явно историческом/поясняющем тексте.
- [x] 5.2 Выполнить `openspec validate "clarify-de-architecture-and-specs" --strict`, `openspec validate --specs --strict`, `git diff --check` и отдельную whitespace-проверку новых файлов; выполнить `bash -n "install.sh"` как синтаксическую проверку. Проверка: команды завершаются без ошибок, результаты не выданы за запуск или тестирование DE.
- [x] 5.3 Проверить `git status --short` и diff всех изменений; передать пользователю результаты и оставшиеся пробелы. Проверка: изменения ограничены документами, конфигурацией OpenSpec, пятью основными спеками и артефактами change; QML, установщик, `extra/`, зависимости, `.agents/`, историческая памятка и модельная пометка не изменены; коммит и архивирование не выполнены.
